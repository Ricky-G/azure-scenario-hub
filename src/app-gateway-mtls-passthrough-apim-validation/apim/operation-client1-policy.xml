<!--
  Operation policy: /client1  (path A).
  Runs AFTER the API-scope inbound (via <base/>), which has already
  established trust and identified the calling client. Here we enforce
  PER-CLIENT AUTHORIZATION: only the client that owns cert1 may use path A.
-->
<policies>
  <inbound>
    <base />
    <choose>
      <when condition='@((string)((Newtonsoft.Json.Linq.JObject)context.Variables["auth"])["client"] != "client1")'>
        <return-response>
          <set-status code="403" reason="Not authorised for this resource" />
          <set-header name="Content-Type" exists-action="override">
            <value>application/json</value>
          </set-header>
          <set-header name="X-Evidence-Decision" exists-action="override">
            <value>DENY_AUTHZ</value>
          </set-header>
          <set-header name="X-Evidence-Client" exists-action="override">
            <value>@((string)((Newtonsoft.Json.Linq.JObject)context.Variables["auth"])["client"])</value>
          </set-header>
          <set-body>@{
              var a = (Newtonsoft.Json.Linq.JObject)context.Variables["auth"];
              var o = new Newtonsoft.Json.Linq.JObject();
              o["decision"] = "DENY_AUTHZ";
              o["reason"] = "Authenticated client is not authorised for path A (client1)";
              o["authenticatedClient"] = (string)a["client"];
              o["requiredClient"] = "client1";
              o["presentedThumbprint"] = (string)a["thumbprint"];
              return o.ToString();
          }</set-body>
        </return-response>
      </when>
    </choose>
    <return-response>
      <set-status code="200" reason="OK" />
      <set-header name="Content-Type" exists-action="override">
        <value>application/json</value>
      </set-header>
      <set-header name="X-Evidence-Decision" exists-action="override">
        <value>ALLOW</value>
      </set-header>
      <set-header name="X-Evidence-Client" exists-action="override">
        <value>client1</value>
      </set-header>
      <set-header name="X-Evidence-AppGw-Verify" exists-action="override">
        <value>@((string)((Newtonsoft.Json.Linq.JObject)context.Variables["auth"])["appGwVerify"])</value>
      </set-header>
      <set-body>@{
          var a = (Newtonsoft.Json.Linq.JObject)context.Variables["auth"];
          var o = new Newtonsoft.Json.Linq.JObject();
          o["decision"] = "ALLOW";
          o["client"] = "client1";
          o["path"] = "A";
          o["message"] = "Hello client1 - authenticated by client certificate and authorised for path A";
          o["presentedThumbprint"] = (string)a["thumbprint"];
          o["mode"] = (string)a["mode"];
          o["appGwVerify"] = (string)a["appGwVerify"];
          o["chainOk"] = (bool)a["chainOk"];
          return o.ToString();
      }</set-body>
    </return-response>
  </inbound>
  <backend>
    <base />
  </backend>
  <outbound>
    <base />
  </outbound>
  <on-error>
    <base />
  </on-error>
</policies>
